# Topic 3G  AJAX Fundamentals: Using PHP

## Key Concepts
- AJAX: asynchronous vs synchronous requests
- `fetch()` API (modern) vs `XMLHttpRequest` (classic)
- PHP JSON endpoints (`header('Content-Type: application/json')`)
- `json_encode()` / `json_decode()`
- Background updates without full page reload
- `GET` / `POST` via AJAX

## Self-Study Reference
Review **[Module 3G](../Study/Unit-3.pdf#page=22)** in the study material before attempting these questions.
