# Event Handling

## Key Concepts
- Registration: inline attributes, DOM property, `addEventListener`
- The event object (`e.type`, `e.target`)
- `e.preventDefault()`
- Event bubbling and `e.stopPropagation()`

## Self-Study Reference
Review **Section 9: Event Handling** in the Unit 1 notes.
