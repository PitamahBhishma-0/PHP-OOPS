# Topic 2J  MySQL Subqueries: IN, EXISTS

## Key Concepts
- Subquery in `WHERE` clause with `IN`
- Subquery with `EXISTS` / `NOT EXISTS`
- Subquery returning a single value (scalar subquery)
- `SELECT AVG(age)` as a subquery
- Comparing performance: `IN` vs `EXISTS`

## Self-Study Reference
Review **[Module 2J](../studymaterial/Unit2_Server_Side_Scripting_Complete.md#module-2j)** in the study material before attempting these questions. Requires the **[db_setup.sql](../db_setup.sql)** to be run first.
